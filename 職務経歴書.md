# 職務経歴書

> 本書は社外公開向けに、社内のプロジェクト名・機能コードネーム・チケットID・個人名・固有ID等を一般化／削除したものです。

---

## 基本情報

| 項目 | 内容 |
|---|---|
| 氏名 | toshichanapp |
| 在籍企業 | 株式会社スペースリー |
| 事業内容 | 不動産・建築向け VR / 360°パノラマコンテンツ作成・配信 SaaS「スペースリー」 |
| 在籍期間 | 2021年11月〜現在 |
| 職種 | プラットフォームエンジニア |
| 所属 | 開発本部（特定チームに限定せず横断的に従事） |

---

## 職務要約

不動産・建築向け VR/360°パノラマ SaaS「スペースリー」を提供する株式会社スペースリーにて、**Webアプリケーション開発（Ruby on Rails / Vue.js）からクラウドインフラ構築・運用（AWS / GCP、Terraform による IaC）まで一気通貫で担当するフルスタック／プラットフォームエンジニア**。

2021年11月の参画以降、約4.5年間で **5リポジトリ・計1,838件のプルリクエストをマージ**。新規プロダクト機能の設計〜実装、数年がかりの基盤移行（EC2→ECS/Fargate）、データ分析基盤の新規構築と高速化、外部システム連携基盤のゼロからの立ち上げ、可観測性（SLO）基盤の整備、コード品質・技術的負債の継続的返済まで、**「機能開発・基盤刷新・運用・廃止」のソフトウェアライフサイクル全体**を主導的に担ってきた。

強みは以下の4点。

- **フルスタックな実行力** — アプリ層とインフラ層の双方を横断し、一つの目的（移行・新機能・基盤構築）を単独でも完遂する。
- **安全な移行・運用力** — Terraform の state 移行や DB メジャーバージョンアップ等、破壊的になりうる変更を「影響ゼロ（destroy 0件）」で計画的に遂行する。
- **障害対応力** — エラー監視・APM のログを起点に根本原因を特定し、対症療法でなく設計改善まで結びつける。
- **堅実なリリース運用** — 並行実装→比較検証→切替→旧削除の段階的手法を採り、リスクを検知したら即 Revert して可逆性を担保する。

---

## 活動データ（マージ済みPR、2021–2026）

| 担当領域 | マージ済みPR | 主な期間 |
|---|---:|---|
| 主力Webアプリ（Ruby on Rails + Vue.js） | **1,399** | 2021–2026 |
| AWSインフラ（Terraform IaC） | **328** | 2022–2026 |
| 不動産情報基幹システム連携 Chrome拡張 | **82** | 2025–2026 |
| GCPインフラ（Terraform IaC） | **15** | 2024–2026 |
| AWS Lambda関数群モノレポ（サーバレス） | **14** | 2024–2026 |
| **合計** | **1,838** | **約4.5年** |

> 「実際に出荷・反映された成果」を示すため、マージ済みPRのみを集計。

---

## 保有スキル・技術スタック

**言語**: Ruby / JavaScript / TypeScript / Go / Python / HCL(Terraform) / SQL

**バックエンド**: Ruby on Rails（STI、ActiveJob、ActiveRecord、Multiple Database / リードレプリカ、ActiveAdmin、Devise、Pundit、Doorkeeper(OAuth 2.0/PKCE)、Zeitwerk、Arel）、Sidekiq / **Sidekiq Pro（Batch）**、Unicorn→Puma

**フロントエンド**: Vue.js 2 / 3（Composition API、`<script setup>`、Vuetify、vue-router）、TypeScript、Vite、axios、**Chrome拡張（WXT / Manifest V3 / Service Worker）**

**インフラ（AWS）**: ECS/Fargate（オートスケール、ECS exec、run-task）、EC2、Lambda / **Lambda@Edge**、Step Functions、EventBridge Scheduler、S3 / **S3 Inventory**、RDS / Aurora MySQL、CloudFront、ALB、Route53、ACM、Cognito、IAM / **OIDC フェデレーション**、KMS、Secrets Manager（自動ローテーション）、SSM Parameter Store、ECR、EFS、SQS、SNS、CloudWatch、**Security Hub**、CloudTrail、Organizations（マルチアカウント）、EC2 Image Builder、Bottlerocket

**インフラ（GCP）**: BigQuery、IAM、**Workload Identity Federation（クロスクラウド認証）**、Service Account、Google Maps Platform、Service Usage API

**IaC / デプロイ**: **Terraform / HCP Terraform**（workspace分割、import / removed / moved blocks、state 移行）、ecspresso、lambroll、CloudFormation（SAR）、Ansible

**データ / 分析**: MySQL、**DuckDB**、Parquet、Redis / Valkey、Elasticsearch

**CI/CD**: **GitHub Actions**（OIDC、reusing workflows、SHA pinning）、CircleCI、Docker、Trivy、Dependabot、Danger

**可観測性**: **Datadog**（APM、**SLO / エラーバジェット**、Log Pipeline、Monitor、Dashboard）、エラー監視SaaS、fluentd、CloudWatch Logs

**テスト / 品質**: RSpec、FactoryBot、**TestProf**、SimpleCov、Playwright、Capybara、Rubocop、ESLint / Prettier、prosopite（N+1検知）、brakeman

**セキュリティ**: OAuth 2.0 / PKCE、Workload Identity Federation、Security Hub、脆弱性診断対応、SPF / DKIM / DMARC、zxcvbn

**その他**: VR/360°パノラマビューア連携、Google Maps Platform、公的地理データ（国土地理院 / 国土数値情報 / OpenStreetMap）、AI活用開発（Claude Code）

---

## 主要実績ハイライト

### 1. EC2 → ECS/Fargate への全面移行（2023年4月–2025年10月、アプリ＋インフラ横断・主導）

主力モノリス（Rails アプリ）を EC2 から ECS/Fargate へ移行する2年超のプロジェクトを、**アプリ層・インフラ層の両方にまたがって主導**。

- **CI/CD基盤**: GitHub Actions から AWS へ **OIDC フェデレーション**で認証する基盤を構築し、長期アクセスキーを廃止。Docker ビルドの GitHub Actions ワークフローを `workflow_call` で再利用可能化し、image build / assets build / 脆弱性スキャン / sidecar をモジュール化。デプロイを ecspresso ベースに統一し、DB マイグレーションをデプロイから分離して並列化。
- **Graceful Shutdown / コンテナ化対応**: ECS デプロイ時の RDS コネクションのぶつ切りと、ジョブワーカーのジョブ揮発を根治。Web サーバに `signal.trap` を追加して SIGTERM→QUIT で graceful shutdown 化、init プロセス（tini）を **PID 1 で正しく動作**させ（"not running as PID 1" 警告を解消）、停止猶予を 120 秒へ延長。
- **常駐プロセスの刷新**: Sidekiq 8 化に伴う Rack 3 移行で非互換となった Unicorn を、メンテ継続中の **Puma へ移行**。デプロイ環境変数を SSM Parameter Store 駆動化。
- **インフラ側受け皿**: ECS オートスケール（Web / ジョブワーカー、キュー長カスタムメトリクス契機）、ECS デプロイ成否の Slack 通知（EventBridge→SNS→ChatOps）、本番リリース後「1週間無障害」を根拠とした旧 EC2 撤去判断、旧 EC2 向け設定・デプロイスクリプト・旧 CI パイプラインの計数千行削除。

**技術**: ECS/Fargate, ecspresso, GitHub Actions(OIDC / reusing workflow), ECR, Unicorn/Puma, tini, Sidekiq, SSM Parameter Store, ALB, EventBridge, SNS

### 2. ストレージ使用量分析基盤の構築と DuckDB 移行による大幅高速化（2025年5月–2026年3月、アプリ＋インフラ＋Lambda横断）

S3 使用量を**会社別に集計・可視化するレポート基盤をゼロから構築**し、その後アーキテクチャを全面刷新して処理時間を劇的に短縮した。

- **初代（CSV + Rails + RDS）**: S3 Inventory(CSV)→RDS インポート→会社ID 紐付け→サマリ生成→管理画面表示のパイプラインを実装。会社別使用量ランキング・推移グラフ・異常値検出・効率スコア／コスト推定までを提供。
- **本番 OOM の克服**: 大容量 CSV の全件メモリ読み込みで OOM が発生 → `CSV.parse` から **ストリーミング方式（`CSV.new`）** へ変更し、バッチ毎のインクリメンタル保存＋GC 頻度最適化で解消。
- **DuckDB への全面移行**: ActiveRecord 経由 INSERT のボトルネックを根本廃止。**DuckDB の MySQL extension で RDS リードレプリカに直接 ATTACH**、`read_parquet` で S3 上の Parquet を直接読み取り、中間テーブルを撤廃。16 種のレコードタイプの UNION ALL で Rails のマッピングロジックを SQL へ移植し、ポリモーフィック関連も再現。実行基盤は **ECS Fargate ARM64 (Graviton) 2vCPU/16GB**、ユニットテスト 38 件。オーケストレーションも 6 ステップ → 1 ステップへ簡素化。
- **負債解消**: 移行完了に伴い Rails 側 ETL 一式（サービス / モデル / バッチタスク / 中間テーブル）を約 4,000 行削除。

**定量成果**: ActiveRecord 経由 INSERT のボトルネックを根本廃止し、6ステップのパイプラインを DuckDB 単一タスクへ簡素化。**レポート出力時間を 約1週間 → 約9分 に短縮**。対象データ規模は **約2億行（Parquet）**。
**技術**: DuckDB, Parquet, S3 Inventory, ECS Fargate(Graviton/ARM64), Step Functions, RDS(リードレプリカ), Ruby GC, Vue3 SPA

### 3. 不動産情報基幹システム連携基盤のゼロからの立ち上げ（2025年11月–2026年4月、サーバAPI＋Chrome拡張）

**不動産情報基幹システム**から物件データを自社プロダクトへ取り込む新規プロダクト基盤を、**サーバAPI側とクライアント拡張側の両方をほぼ単独で**設計・実装。

- **アーキテクチャ転換の完遂**: 当初は Playwright + ECS Fargate の**サーバサイドスクレイパー**（初期 +6,671 行）として開始したが、スクレイパーを全廃（-10,231 行）し **Chrome拡張（WXT + TypeScript / Manifest V3）へ全面ピボット**。1,200 行のモノリシックなスクリプトを 50 行に縮小、`any` 型を全除去して strict 化、外部レビュー指摘 32 件を完全対応。
- **OAuth 2.0 認証基盤**: サーバ側に **Doorkeeper で OAuth 2.0 プロバイダ（Authorization Code + PKCE 必須）**を構築。「OAuth Bearer + 従来トークン」のデュアル認証で後方互換を維持。拡張側は `chrome.identity.launchWebAuthFlow` で PKCE フローを実装し、最終的に OAuth へ統一。
- **堅牢な双方向同期**: Background Service Worker が `chrome.cookies` で外部システムのセッションを取得し API を直接呼出。**アクセストークン（JWT、約13時間で失効）の `exp` を事前検証**し、残り60秒以内なら自動リフレッシュ、認証エラー時は再ログインへ誘導するフェイルセーフを実装。連携先が SPA で Content Script が再マウントされない問題に対し、`popstate` + MutationObserver + ポーリングの多層防御で対応。
- **サーバ側非同期化**: インポートを Sidekiq Job 非同期化（同期バリデーション→一時保存→enqueue→202）、初回大量書き込みの OOM を ECS run-task の種別分割実行＋GC で回避。

**定量成果**: 直接インポートの処理時間 — 初回（新規作成）約1.4秒 / 再インポート（更新）約0.2秒 / PDF 処理を含めて約6.5秒。一括連携を逐次→3並列・上限10件化。
**技術**: Chrome拡張(WXT/MV3), TypeScript, OAuth 2.0/PKCE, Doorkeeper, Sidekiq, Step Functions, S3, JWT, MutationObserver

### 4. ジョブ実行基盤（Sidekiq）の進化（2024年8月–2026年4月）— 用途別キュー分離・Sidekiq Pro 導入

ECS 化により「1プロセスで全部」だったジョブ実行を「用途別に分離」へ進化させた。

- **構造的なスケール事故の解消**: 「オートスケールが全キュー合計サイズで判定されるため、大量エンキュー時にスケールアウト直後スケールインが走り、2分以上かかるジョブが待機列に巻き戻され、冪等でないジョブでデータ不整合が起きる」という課題を自ら明確に定義し、**短時間・大量エンキュー系のジョブを専用キュー＋専用 ECS に分離**、キュー別にオートスケール頻度を設定して構造的に解決。
- **Sidekiq Pro 導入**: 巨大ジョブ分割・監視連携のため Pro を導入し、プロジェクト複製処理を **Sidekiq Batch の3フェーズ並列ワークフロー**（基底クラス＋12ワーカー）へ全面刷新。
- **スケジューラ専用コンテナ**: Fargate の image cache 非対応によるコールドスタート制約を、毎分ジョブはスケジューラコンテナ＋ユニークジョブ制御で、その他は EventBridge Scheduler で使い分け。

**技術**: Sidekiq 7/8, Sidekiq Pro(Batch/callbacks), sidekiq-scheduler, sidekiq-unique-jobs, ECS オートスケール, EventBridge Scheduler

### 5. 可観測性・SLO 基盤の構築（Datadog、2024年1月–2026年3月）

個別アラートしかなかった監視体制に、**エラーバジェットベースの SLO 監視基盤をゼロから Terraform で構築**。

- Web（p95 / p99 レイテンシ、5xx 率、可用性 7日99.95% / 30日99.9%）とジョブワーカーの SLO・モニター・ダッシュボードを IaC として包括実装。
- 全ジョブ一括で形骸化していた SLO を、**キュー別の metric-based 成功率 SLO** へ高度化（旧リソース参照を外す2段階構成で安全に移行）。
- ECS コンテナログの構造化（Grok Parser / Status Remapper）、ログメトリクスの IaC 化、ジョブ実行時間の CloudWatch Logs 記録 middleware を整備。

**技術**: Datadog(SLO / Monitor / Dashboard / Log Pipeline / APM trace metrics), Terraform(for_each / import), CloudWatch

### 6. メール到達性・認証の改善（SPF / DKIM / DMARC、2023年12月–2025年11月）

自社メールの到達性・なりすまし対策を改善。

- メール送信を DKIM 非対応の旧配信サービスから **DKIM 対応の Mailgun へ移行**。「5,000通/日超では SPF/DKIM 両対応が必要、自社は通常1,000通/日未満だが時にスパイクがある」とリスクを評価した上で判断。
- インフラ側で送信用 DNS（Mailgun）・受信認証（Google Workspace）・SPF レコード整理・各種メール検証 DNS を Terraform 管理化。

**技術**: ActionMailer, Mailgun, SPF/DKIM/DMARC, Route53(TXT/MX), Google Workspace

### 7. 認証・セキュリティの全社的強化（2023年5月–2026年5月）

- **同時ログイン制御**: セッションID による複数端末同時ログインの制御を Web / SSO / モバイルアプリの3経路すべてに実装。フロントは axios interceptor で 401（認証切れ）時にログイン画面へ自動リダイレクト（散在する API 呼出を共通レイヤへ集約する TypeScript 化リファクタも実施）。
- **アカウントロック / パスワード強度**: 脆弱性診断指摘を受け、ログイン失敗回数によるアカウントロックと、**zxcvbn** によるパスワード強度推定（日本語化対応）を実装。
- **クラウド認証のセキュア化**: Google Maps Platform のサーバ認証を **API key → OAuth 2.0 + Workload Identity Federation** へ移行。Maps 専用のサービスアカウントを新設して既存用途と権限境界を分離しつつ、信頼相手が同一のため WIF Pool/Provider は流用する設計判断を採用。
- **セキュリティ標準対応**: クラウドセキュリティ基準（Security Hub 等）への準拠、特権コンソールログインの通知、脆弱性診断環境の構築、GitHub Actions の commit SHA ピン留めによるサプライチェーン対策。
- **DB ユーザのシークレット自動ローテーション**: オンラインスキーマ変更ツール用に新設した DB ユーザを Secrets Manager + Lambda で 30 日サイクル自動ローテーション。

**技術**: Devise(lockable), zxcvbn, OAuth 2.0, Workload Identity Federation, Cognito, Security Hub, Secrets Manager(rotation), Trivy

### 8. VR研修コンテンツ機能の大型リニューアル（2021年11月–2022年6月、新規プロダクト・主担当）

参画初期の最大実績。自社の VR/360°研修プロダクトの編集機能を全面リニューアルする大型プロジェクト（約100 PR）を、**STI によるデータモデリング・Rails API・Vue.js 編集UI・VR/360°ビューア連携・非同期ジョブ移行・段階リリースまで一気通貫で主担当**。本番で旧画面／新画面を並行運用する無停止移行も制御した。

- プロジェクト複製機能のコネクションプール枯渇障害を、エラー監視のログから `ActiveRecord::ConnectionTimeoutError` を特定し、重い複製処理をサービス分割＋途中でコネクションを明示クローズする設計で根治。

**技術**: Rails(STI / ActiveJob), Vue.js(Vuetify), VR/360°ビューア, MySQL, S3, エラー監視SaaS

### 9. CS運用基盤・管理画面の刷新（2026年1月–2026年4月）

カスタマーサポート（CS）チームの運用効率化のため、**管理画面（ActiveAdmin）の継続改善と、利用者向け Vue 2 画面に混在していた管理者専用機能の集約・責務分離**を推進（約55 PR）。CS サポート用アカウントが利用者数上限を消費する問題を専用ロールで解決し、ポイント管理機能を Vue 3 のタブ画面（検索＋ページネーション＋同期/非同期 CSV）へ集約した。

**技術**: ActiveAdmin, Ransack, Kaminari, Vue 3(Composition API), Pundit, Devise(lockable)

### 10. パフォーマンス改善・大規模技術的負債返済（2021年11月–2026年5月・通年）

- **会社状況レポートのバッチ最適化**: 管理画面の全件 CSV 出力の OOM・タイムアウトを、過去に9個の重大バグで見送られていた実装を全修正して根本解決。**クエリ数を約1,800（100社）→約25 に削減**。並行実装→比較検証→切替→旧削除という安全なリリース手法を採用。
- **CI/テスト高速化**: TestProf 導入＋約200箇所の `create→build_stubbed` / `let_it_be` 移行で、**spec 単位のセットアップ時間を最大 −91%**（代表例: あるクエリ系 spec で 6.7秒→0.6秒）。flaky なビューア系テストを CI から分離し Playwright へ移行して安定化。
- **コード品質基盤**: Rubocop を全面導入し 1 Cop = 1 PR で段階解消（約74 PR）、N+1 検知を偽陽性ゼロのツールへ刷新、静的セキュリティ解析の導入と外部コマンド実行の排除（command injection 対策）。
- **アップグレード**: Rails 5.2→6.1（Multiple Database / Zeitwerk 対応含む）、Ruby 2.6→3.3、MySQL 5.7→8.0、Sidekiq 6→8 を、Revert / 再適用を含む慎重な本番反映で完遂。
- **デッドコード削除**: レガシー Vue 2・旧 API・未使用バッチ・旧ビューア向け生成処理を継続的に削除（単発で -2,266 行、-4,037 行等）。
- **モジュール分割**: 1,463 行に肥大化したスクリプトを**ファクトリ関数パターン（DI）で7サブモジュールに分割**し、オーケストレーターを 107 行へ（公開 API 不変）。

### 11. 物件周辺情報マップ機能（2026年5月、Google Maps Platform + 公的地理データ統合）

物件ガイド向けプロトタイプとして、物件周辺の駅・バス停・徒歩経路・公共施設・浸水ハザード・商業施設を地図表示する新機能を構築（+約15,000 行）。

- ジオコーディングを **国土地理院**、最寄り駅を **国土数値情報（約3MB の GeoJSON をインメモリ index 化）**、バス停を **OpenStreetMap（Overpass）**、徒歩経路を Google Routes API、商業施設を Google Places API (New)、公共施設・浸水想定区域を**公的な不動産情報ライブラリ API** で統合。±1km の表示範囲制限とズーム制限で Maps の課金（Map Loads）を抑制。
- 複数の地図サービス・データソースを比較選定し、課金体系（SKU）を踏まえた構成を設計。

**技術**: Google Maps JS / Routes / Places API, 国土地理院 / 国土数値情報 / OpenStreetMap / 不動産情報ライブラリ, Vue 3 + TypeScript, Workload Identity Federation

### 12. インフラのコード化・運用基盤（Terraform / HCP Terraform、2023年4月–2026年5月）

AWS インフラを**単独で IaC 化・運用**。設計から運用・移行・廃止までライフサイクル全体を担当。

- **HCP Terraform ワークスペースのアカウント別分割**: 複数アカウント／責務が混在したワークスペースを、import / removed / moved ブロックを駆使して**「destroy 0件」で安全に分割移行**（監査ログ系で 41 件、API 系で 67–68 件のリソースを import）。
- **管理画面のサブドメイン分離**: 認証 Cookie 肥大化で利用者画面が表示できない不具合を、管理画面を専用サブドメインへ分離して解決（ALB リスナールール＋Cognito の多段制御）。
- **DNS の段階移行**: Route53 ベースの DNS を Phase 1〜4 で安全に移行（CNAME リダイレクト・NS 権限移譲・旧レコード削除）。
- **CloudFront 署名付き URL 基盤**: 公開状態だったストレージバケットをプライベート化し CloudFront 経由配信へ（OAC、キーグループ、商用→開発環境へ展開）。
- **BigQuery / GCP 運用**: ログテーブルのスキーマを Terraform 管理化、BigQuery 閲覧権限付与・退職者オフボード・drift 解消を IaC で継続運用。
- **AI 活用**: 各ワークスペースに AI 開発用コンテキスト（CLAUDE.md）を整備し、独自 slash command を追加して Claude Code による開発効率化を推進。

**技術**: Terraform / HCP Terraform(import / removed / moved / state 移行), AWS Organizations, Cognito, ALB, Route53, ACM, CloudFront(OAC), BigQuery, Claude Code

---

## 年表（時系列サマリ）

| 時期 | 主な取り組み |
|---|---|
| **2021.11–2022** | VR研修コンテンツ機能の大型リニューアル（新規プロダクト主担当）/ Rubocop 全面導入 / Rails 6.1・Ruby 2.7 アップグレード / テストカバレッジ向上 / レガシー大規模削除 / CI 高速化 / S3 画像処理の堅牢化 |
| **2023** | 認証セキュリティ強化（同時ログイン制御 / アカウントロック / パスワード強度）/ DB スケーリング（リードレプリカ / MySQL 8.0）/ フロント API 通信層リファクタ / 不動産コンバータシステム連携の堅牢化とインシデント対応 / メール基盤の Mailgun 移行 / コンテナ化に向けた CI/CD・OIDC 基盤構築 |
| **2024** | EC2→ECS/Fargate 移行のデプロイ基盤主導 / Graceful Shutdown（PID 1 問題）/ バッチのコンテナ化（スケジューラ・EventBridge）/ ストレージコスト削減 / Datadog ログ可観測性 / ECS オートスケール / VPN 認証局刷新 |
| **2025** | ECS 移行の総仕上げ（Puma 化・Sidekiq v8/Pro・キュー分離）/ ストレージ使用量分析基盤の新規構築と本番 OOM 克服 / 不動産情報基幹システム連携基盤の立ち上げ / Datadog SLO 基盤構築 / HCP Terraform ワークスペース分割 / セキュリティ強化 / DNS 段階移行 |
| **2026** | CS 運用管理画面の刷新 / 不動産情報基幹システム連携 API 基盤（OAuth）/ 使用量分析基盤の DuckDB 全面移行（出力時間 約1週間→約9分）/ Sidekiq Batch 並列化 / CI 高速化（セットアップ時間 −91%）/ 物件周辺情報マップ新機能（Google Maps）/ クラウド認証の Workload Identity Federation 移行 |

---

*技術的詳細や各実績の定量データは面談時に補足可能です。*
